# Contributing

Open an issue before proposing a new agent adapter or persistence behavior. Keep changes read-only by default, avoid new dependencies unless justified, add focused tests for parsing and safety behavior, and never include real user task logs in fixtures.

Run the documented checks before opening a pull request. Maintainers merge reviewed changes; automation must not auto-merge.
